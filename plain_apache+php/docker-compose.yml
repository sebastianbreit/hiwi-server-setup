version: "3.7"

services:
    plain-apache-environment:
        #container_name: plain-apache
        build:
            context: ./
            dockerfile: Dockerfile
        volumes:
            - ./:/var/www/html/
        restart: always
        networks: 
            - "reverse-proxy-net"
        expose:
            - "6000"
networks:
  reverse-proxy-net:
    external: true
